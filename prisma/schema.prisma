generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider  = "postgresql"
}

model Vocabulary {
  id     Int    @id @default(autoincrement())
  userId String
  word   String
}
